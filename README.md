# pulsar-hammer
A lightweight Go application that can be used for performance testing on apache pulsar clusters. Integrated with DAPR gRPC

# How to setup a local pulsar test cluster on minikube
- `cd test`
- `chmod +rwx install.sh`
- `./install.sh`